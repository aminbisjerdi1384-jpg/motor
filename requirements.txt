# Core app dependencies (run: streamlit run app.py)
streamlit>=1.30
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
reportlab>=4.0
openpyxl>=3.1

# Only needed if you run download_cwru_data.py locally to fetch the real
# CWRU dataset (this script must be run on a machine with internet access).
requests>=2.31
